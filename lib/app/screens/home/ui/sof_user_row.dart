import 'package:flutter/material.dart';

class SofUserRow extends StatelessWidget {
  const SofUserRow({super.key});

  @override
  Widget build(BuildContext context) {
    return const Row(
      children: [Text("hello")],
    );
  }
}
